<!DOCTYPE html>

<title>Hello SearchController!</title>

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<h1>Search Results for "{{ sectionName }}"</h1>

    {% if links is empty %}
        <p>No links found for this search.</p>
    {% else %}
        <ul>
            {% for linkArray in links %}
                {% for key, value in linkArray %}
                    <li><a href="{{ value }}">{{ value }}</a></li>
                {% endfor %}
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}
