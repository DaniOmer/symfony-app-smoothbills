{% props bg = 'outline', rounded = 'md' %}

{% set button = cva({
    base: 'button ',
    variants: {
        bg: {
            filled: 'bg-primary text-white',
            outline: 'bg-transparent',
        },
        rounded: {
            sm: 'rounded-small',
            full: 'rounded-full',
        }
    }
}) %}

<button {{ attributes.defaults({type:"button"}) }} class="px-6 py-2 border border-primary {{ button.apply({ bg, rounded }) }}">
    {% block content %}
        Valider
    {% endblock %}
</button>
